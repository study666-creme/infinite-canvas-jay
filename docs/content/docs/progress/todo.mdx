---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

## 视频播放（高优先级）

- [ ] **画布内稳定播放 Seedance / 即梦网关返回的视频**：当前常报 `无法下载.../v1/media/fetch` 或 `<video>` 解码失败。
- [ ] 验证 `jimeng-free-api-all` 的 `/v1/media/fetch` 与 `/contents/generations/tasks/{id}/content` 与前端 `video.ts` 路径一致。
- [ ] 网关 Seedance 任务持久化（避免重启后 `/content` 404，只能依赖远程 URL + fetch）。
- [ ] 生成失败 / 播放失败时的用户可见诊断（Network 请求摘要或复制排查信息）。

详见 [视频播放与故障排查](/docs/overview/video-playback)。

## 其它

- 本地 Agent 接入后续：按[本地 Agent 接入规划](/docs/progress/local-agent-integration-plan)把 Claude Code CLI Adapter 升级为 Claude Agent SDK Adapter，并完善工具队列体验。

## 性能与本地数据结构

- [ ] 拆分画布本地持久化：当前 `infinite-canvas:canvas_store` 会一次性读取所有项目详情，项目/节点/会话变多后 `/canvas` 列表首屏会慢。建议拆成项目索引和项目详情两层存储，并设计旧数据兼容迁移。
- [ ] 评估“我的素材”首屏懒加载：`asset_store` 恢复时仍可能解析大量图片/视频 URL，素材量大时可能拖慢素材库或资产选择弹窗；后续可改成列表只读元数据，插入/预览时再解析媒体。
- [ ] 为生图历史增加大数据量回归样例：至少覆盖 100+ 图片记录、视频记录、参考图记录，验证 `/image` 和 `/video` 首屏不会一次性读取所有原图。
